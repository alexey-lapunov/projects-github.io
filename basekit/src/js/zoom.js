/*
function resizeWindow() {
    var windowWidth = document.documentElement.clientWidth;

    if (windowWidth > 600) {
        $('.information-picture').zoom({
            url: '../images/product-4-zoom.jpg'
        });
    } else {
        $('.information-picture').trigger('zoom.destroy');
    }
}

window.addEventListener('resize', resizeWindow);
resizeWindow();
*/
